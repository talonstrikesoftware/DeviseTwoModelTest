<% content_for :top_content do %>
  <div class="jumbotron">
    <div class="container">
      <h1>Admin Welcome!</h1>
<!--      < if admin_signed_in? %>
        <p>Admin Register to get started.</p>
        <p>
          <= link_to 'Admin Register', new_admin_registration_path, class: 'btn btn-primary btn-lg' %>
        </p>
      < end %> -->
    </div>
  </div>
<% end %>